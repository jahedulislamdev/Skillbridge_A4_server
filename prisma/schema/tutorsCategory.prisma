// tutor category (One teacher can teach multiple subjects)
model TutorCategory {
  tutorId    String
  categoryId String
  tutor      Tutor    @relation(fields: [tutorId], references: [id])
  category   Category @relation(fields: [categoryId], references: [id])

  @@id([tutorId, categoryId]) // composite PK
}
