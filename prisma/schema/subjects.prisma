// subject category
model subjects {
  id            String         @id @default(uuid())
  name          String         @db.VarChar(50)
  tutorSubjects TutorSubject[]

  @@index([name], name: "idx_sub_name")
  @@map("subjects")
}
